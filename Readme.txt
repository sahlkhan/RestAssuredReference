Feature of the project:

1 This project is able to automate, execute and validate.
   Rest : POST, PUT , PATCH , Delete and GET
   Soap : POST, GET

2 Contructed the baseURL, requestbody using from reqres website and used when given,     when and the method. I have created a package under that package created the different methods test classes.

3 Test execution is driven from from the class and give the response in console.

